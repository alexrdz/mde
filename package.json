{
  "name": "mde",
  "version": "0.1.0",
  "description": "A simple darkdown editor with local storage.",
  "private": true,
  "dependencies": {
    "marked": "^0.3.19",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-scripts-tailwindcss": "1.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cp CNAME ./build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
